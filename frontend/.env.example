# Base URL of the FastAPI app (no trailing slash). Used only on the server by the Next.js Route Handler.
# Local: start API with `uv run uvicorn api.index:app --reload` from repo root (default port 8000).
BACKEND_URL=http://127.0.0.1:8000
